# Recruit Workspace

`Recruit` 関連の設計資料と UI モックを LP から切り離して管理するためのフォルダです。

## Structure

- `docs/`: 要件整理、既存ストーリー整理、詳細設計
- `mock/`: 採用ファネルダッシュボードの独立 UI モック

## Start Preview

`mock/` は静的ファイルなので、ワークスペースのルートでローカルサーバーを起動したうえで、
`/recruit/mock/` を開くことで確認できます。

例:

- `http://127.0.0.1:4173/recruit/mock/`
